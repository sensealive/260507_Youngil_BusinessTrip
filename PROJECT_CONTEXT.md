# 프로젝트 컨텍스트

## 목적

영일 직원 출장 등록, 출장자 현황 조회, 관리자 데이터 관리를 위한 정적 웹앱입니다.  
GitHub Pages 같은 정적 호스팅에서 동작하며, 데이터와 인증은 Supabase를 사용합니다.

## 현재 구조

- `index.html`: 관리자/직원 공통 로그인 화면
- `business-trip.html`: 일반 직원 출장 등록 및 출장자 현황 화면
- `admin-business-trip.html`: 관리자 출장 등록 및 전체 출장자 현황 화면
- `admin-manage.html`: 부서, 직원, 업체등록(출장지) 관리 화면
- `src/app.js`: 모든 페이지의 화면 로직
- `src/store.js`: Supabase 테이블 CRUD 및 다운로드/업로드용 데이터 처리
- `src/auth.js`: Supabase Auth 로그인, 로그아웃, 비밀번호 변경
- `src/config.js`: Supabase 프로젝트 URL과 anon/public key, Auth 기본 설정
- `src/styles.css`: 공통 스타일
- `supabase/migrations/*.sql`: Supabase 스키마/RLS 변경

삭제된 구형 JS 파일(`src/main.js`, `src/admin.js`, `src/trips.js`, `src/stats.js`, `src/permissions.js`)은 더 이상 사용하지 않습니다.

## 인증과 설정

- 로그인은 ID + 비밀번호 방식입니다.
- 관리자 ID는 `admin`이며 내부 Auth 이메일은 `admin@project.local`입니다.
- 일반 직원은 `login_id`를 사용하며 내부 Auth 이메일은 `직원ID@project.local` 형식입니다.
- Supabase Project URL과 publishable/anon key는 `src/config.js`의 기본값에서 읽습니다.
- 브라우저 정적 앱 특성상 publishable/anon key는 최종 사용자에게 보일 수 있습니다. 보안은 RLS 정책으로 보호합니다.

## 출장 화면 정책

- 일반 직원과 관리자는 출장자 현황 테이블을 같은 기준으로 봅니다.
- 일반 직원도 전체 직원의 현재/예정 출장 현황을 조회할 수 있습니다.
- 일반 직원은 본인이 등록한 출장만 수정/삭제할 수 있습니다.
- 관리자는 전체 직원의 출장 등록/수정/삭제가 가능합니다.
- 직원 화면에는 `본인 출장만 보기` 토글이 있으며, 켜면 로그인 사용자 본인 출장만 표시합니다.
- 출장자 현황은 출장 시작일이 임박한 순으로 정렬합니다.
- 한 페이지에는 헤더 제외 15행을 표시합니다.

## 출장 필터

- `출장중`: 오늘 날짜가 출장기간 안에 포함되는 출장
- `출장예정`: 시작일이 오늘보다 미래인 출장
- `출장전체`: 현재 진행 중인 출장 + 미래 예정 출장
- `국내출장`: 국가가 국내인 출장
- `해외출장`: 국내가 아닌 출장

## 출장 등록 폼

- 도시명 항목은 사용하지 않습니다.
- 국가 선택은 `국내`를 최상단에 두고 구분선 아래 타 국가를 가나다순으로 표시합니다.
- `기타`는 국가 목록 맨 마지막에 둡니다.
- 방문업체명은 텍스트 입력입니다.
- `업체명 참조` 드롭다운은 업체등록(출장지) 목록을 참고해 입력칸에 복사하는 용도입니다.
- `비고 (선택)`은 한 줄 라벨로 표시합니다.
- `수정&저장`: 선택된 출장 행을 수정합니다.
- `신규저장`: 선택된 행이 있어도 새 출장 일정으로 등록합니다.
- 같은 직원의 다른 active 출장과 기간이 겹치면 저장하지 않습니다.
- 겹침 오류에는 충돌한 출장 기간/국가/업체명을 함께 표시합니다.

## 엑셀 기능

- 출장자 현황 테이블 상단 오른쪽에 `출장 엑셀업로드`, `엑셀 다운로드` 버튼을 둡니다.
- 업로드 양식은 `ref/businesstrip_up.xlsx` 기준입니다.
- 업로드 행에 필수값이 하나라도 비어 있으면 해당 행은 skip합니다.
- 같은 직원의 출장기간이 기존/업로드 중 일정과 겹치면 해당 행은 skip합니다.
- skip/오류 원인은 토스트 팝업으로 10초 표시하고, 가능한 나머지 행 등록은 계속 진행합니다.
- 다운로드는 현재 필터 조건 기준으로 생성합니다.

## 비밀번호 변경

- `business-trip.html`, `admin-business-trip.html`, `admin-manage.html`의 현재접속자 아래에 `[비밀번호 변경]` 버튼을 표시합니다.
- 팝업 입력 항목은 현재 비밀번호, 새로운 비밀번호입니다.
- 새로운 비밀번호는 최소 6자리 이상이어야 합니다.
- 현재 비밀번호가 Supabase Auth에서 검증되면 새 비밀번호로 변경합니다.

## 운영 주의

- `service_role` 키는 저장소, 브라우저 코드, `.env` 어디에도 넣지 않습니다.
- 실제 Supabase에는 `001`부터 `009`까지 마이그레이션이 순서대로 적용되어야 합니다.
- `007`, `008`, `009`는 직원 화면 전체 출장 조회와 로그인 ID 조회 정책에 중요합니다.
- `business-trip.html`과 `admin-business-trip.html`은 출장 테이블 로직을 공유하므로 한쪽 변경 시 다른 쪽 영향도 확인합니다.
