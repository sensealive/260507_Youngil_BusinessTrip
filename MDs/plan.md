# 현재 구현 기준 요약

이 파일은 초기 UI 개편 계획 대신 현재 코드 기준으로 유지합니다. 상세 운영 컨텍스트는 `PROJECT_CONTEXT.md`, 작업 이력은 `WORK_LOG.md`, 남은 일은 `TODO.md`를 우선 참조합니다.

## 페이지

- `index.html`: 로그인
- `business-trip.html`: 직원 출장 등록/조회
- `admin-business-trip.html`: 관리자 출장 등록/조회
- `admin-manage.html`: 부서, 직원, 업체등록(출장지) 관리

## 출장 등록

- 도시명 항목은 사용하지 않습니다.
- 방문업체명은 텍스트 입력입니다.
- `업체명 참조` 드롭다운은 업체등록(출장지) 목록을 참고해 텍스트 입력칸에 값을 복사합니다.
- `비고 (선택)`은 줄바꿈 없이 표시합니다.
- `수정&저장`은 선택된 출장 행을 수정합니다.
- `신규저장`은 선택된 행이 있어도 새 출장으로 등록합니다.
- 일반 직원은 본인 출장만 수정/삭제할 수 있습니다.
- 관리자는 전체 직원 출장 등록/수정/삭제가 가능합니다.

## 출장자 현황

- 직원 화면과 관리자 화면은 출장자 현황 테이블을 같은 기준으로 표시합니다.
- 정렬은 출장 시작일이 임박한 순입니다.
- 페이지당 15행을 표시합니다.
- 필터는 `출장중`, `출장예정`, `출장전체`, `국내출장`, `해외출장`입니다.
- 직원 화면에는 `본인 출장만 보기` 토글이 있습니다.
- 통계 박스는 `출장중`, `출장예정`, `국내출장`, `해외출장` 인원을 표시합니다.

## 국가/업체

- 국가 선택은 `국내`를 맨 위에 두고 구분선 아래 타 국가를 가나다순으로 표시합니다.
- `기타`는 맨 마지막에 표시합니다.
- 업체등록(출장지)은 `admin-manage.html`에서 텍스트 입력으로 관리합니다.

## 엑셀

- 출장자 현황 테이블 상단에 `출장 엑셀업로드`, `엑셀 다운로드` 버튼이 있습니다.
- 업로드 양식은 `ref/businesstrip_up.xlsx` 기준입니다.
- 필수값 누락 또는 출장기간 겹침은 해당 행 skip 후 나머지 등록을 계속합니다.
- 오류/skip 메시지는 10초 토스트로 표시합니다.

## 비밀번호 변경

- `business-trip.html`, `admin-business-trip.html`, `admin-manage.html`의 현재접속자 아래 `[비밀번호 변경]` 버튼을 표시합니다.
- 현재 비밀번호 검증 후 새로운 비밀번호로 저장합니다.
- 새로운 비밀번호는 최소 6자리 이상입니다.

## Supabase

- 실제 Supabase URL/key는 `src/config.local.js`에서 읽습니다.
- `src/config.local.js`는 `.gitignore` 대상입니다.
- 저장소에는 `src/config.local.example.js`만 둡니다.
- `service_role` 키는 브라우저 코드에 넣지 않습니다.
