# [260507_Youngil_BusinessTrip] Project Lessons 기록

## 2026-05-11 작업 회고

### 오늘의 목표

영일 직원 출장 등록, 출장자 현황 조회, 관리자 데이터 관리를 위한 정적 웹앱을 완성한다.

앱은 GitHub Pages 같은 정적 호스팅에서 동작하고, 인증과 데이터는 Supabase를 사용한다. 일반 직원은 본인 출장 등록/수정/삭제와 전체 출장자 현황 조회를 하고, 관리자는 전체 직원·부서·업체·출장 데이터를 관리한다. 엑셀 업로드/다운로드, 출장 기간 겹침 검사, 비밀번호 변경, 공개 배포 설정까지 포함한다.

겉으로 보면 HTML, CSS, JavaScript와 Supabase를 연결하는 단순한 정적 웹앱처럼 보인다. 그러나 실제 작업 중에는 RLS 정책, Auth 계정과 직원 테이블의 연결, 공개 가능한 키와 절대 공개하면 안 되는 키의 구분, 관리자/직원 화면의 권한 차이, 엑셀 업로드 검증, 배포 설정이 서로 맞물리며 여러 번 시행착오가 발생했다.

이 문서는 같은 시행착오를 반복하지 않기 위한 재발 방지 기록이다.

## 실패와 원인

### 1. Supabase RLS 정책이 화면 요구사항보다 좁게 설계됨

초기 정책에서는 일반 직원이 본인 또는 제한된 범위의 출장만 볼 수 있게 되어 있었다. 하지만 실제 요구사항은 일반 직원도 전체 직원의 현재/예정 출장자 현황을 같은 기준으로 조회하는 것이었다. 이 차이 때문에 직원 화면의 출장자 현황 테이블이 일부 데이터만 표시되는 문제가 발생했다.

재발 방지:

- 화면 요구사항을 먼저 권한 매트릭스로 정리한 뒤 RLS를 작성한다.
- `business-trip.html`과 `admin-business-trip.html`에서 같은 조회 기준을 쓰는지 함께 확인한다.
- 직원 화면 전체 출장 조회에는 `007_trips_select_all_authenticated.sql`, `008_employee_trip_table_full_visibility.sql` 적용이 중요하다.

### 2. 로그인용 직원 조회 정책을 되살리지 않아 직원 ID 로그인이 막힘

RLS를 조정하는 과정에서 `employees` 조회 범위가 바뀌었고, 로그인 화면에서 입력한 직원 ID를 조회하지 못하는 상태가 발생했다. 실제 로그인은 Supabase Auth로 하지만, 그 전에 `employees.login_id`로 직원을 찾는 단계가 필요하다는 점을 놓쳤다.

재발 방지:

- 로그인 흐름은 `직원 ID 조회 -> login_email 확인 -> Supabase Auth 로그인 -> profile 조회` 순서로 검증한다.
- RLS 변경 후에는 로그인 전 익명/인증 전 조회가 필요한 데이터가 있는지 반드시 확인한다.
- 직원 ID 로그인 문제는 `009_restore_employee_login_lookup_policy.sql` 적용 여부를 먼저 확인한다.

### 3. Supabase URL/key와 service_role 키의 성격을 혼동함

정적 브라우저 앱에서는 Supabase URL과 publishable/anon key가 사용자에게 보일 수밖에 없다. 이를 비밀 값처럼 숨기려다 설정 방식이 흔들렸고, 반대로 `service_role` 키가 절대 브라우저 코드에 들어가면 안 된다는 점도 명확히 분리해야 했다.

재발 방지:

- `src/config.js`에는 공개 가능한 Project URL과 publishable/anon key만 둔다.
- `service_role` 키는 저장소, 브라우저 코드, `.env`에 넣지 않는다.
- 보안은 키 은닉이 아니라 RLS 정책으로 보장한다.
- 배포 전 `src/config.js`, `.env`, 문서에서 service_role 문자열이 없는지 확인한다.

### 4. 로컬 오버레이 설정과 배포 기본 설정 사이에서 설정 방식이 흔들림

초기에는 `src/config.local.js` 같은 로컬 오버레이를 통해 설정을 분리하려 했지만, GitHub Pages 배포에서는 실제 기본 설정이 `src/config.js`에 있어야 했다. 이 과정에서 로컬에서는 되지만 배포 환경에서는 설정을 못 읽는 방향으로 흐를 수 있었다.

재발 방지:

- 정적 배포에서 반드시 필요한 공개 설정은 `src/config.js`에 둔다.
- 로컬 전용 오버레이를 쓰는 경우에도 배포 경로에서 읽히는 기본값을 먼저 확인한다.
- 설정 변경 후에는 `README.md`와 `docs/Supabase_setup.md`의 안내도 함께 갱신한다.

### 5. 관리자 계정과 직원 계정의 Auth 이메일 규칙을 명확히 고정하지 않음

화면에서는 관리자 ID를 `admin`으로 입력하지만 Supabase Auth에는 `admin@project.local`로 등록해야 한다. 일반 직원도 화면의 `login_id`와 내부 Auth 이메일(`직원ID@project.local`)이 연결되어야 한다. 이 규칙이 흐릿하면 직원 정보 행은 있는데 Auth 사용자가 없어 로그인할 수 없는 문제가 생긴다.

재발 방지:

- 관리자 화면 ID `admin`은 Auth 이메일 `admin@project.local`로 매핑한다.
- 직원 ID는 소문자로 정규화하고 Auth 이메일은 `직원ID@project.local` 형식으로 만든다.
- 직원 등록/수정 시 `employees.login_id`, `login_email`, `auth_user_id`가 함께 맞는지 확인한다.
- 로그인 오류 메시지에는 직원 행 문제와 Auth 계정 문제를 구분해 안내한다.

### 6. 비밀번호 변경 후 `must_change_password` 해제를 직접 UPDATE로 처리하려 함

직원 본인은 RLS 때문에 `employees` 행을 자유롭게 업데이트할 수 없다. 비밀번호 변경 후 `must_change_password`를 해제하려면 본인 행의 해당 플래그만 바꾸는 RPC가 필요했다.

재발 방지:

- 본인 제한 업데이트는 직접 테이블 UPDATE보다 명시적인 RPC를 우선 검토한다.
- `002_mark_own_password_changed_rpc.sql` 적용 여부를 확인한다.
- 비밀번호 변경 테스트는 Auth 비밀번호 변경 성공과 `must_change_password` 해제까지 함께 본다.

### 7. 관리자/직원 출장 저장 로직의 권한 차이를 충분히 분리하지 않음

직원은 본인 출장만 수정/삭제할 수 있고, 관리자는 전체 직원의 출장을 등록/수정/삭제할 수 있다. 같은 출장 폼 로직을 공유하면서도 권한 조건이 다르기 때문에, 선택된 행을 직원이 잘못 수정하거나 삭제할 수 있는 위험이 있었다.

재발 방지:

- 직원 화면에서는 선택된 출장의 `employee_id`가 로그인 사용자와 같은지 저장/삭제 직전에 다시 검사한다.
- 관리자는 대상 직원 선택값을 기준으로 등록/수정한다.
- UI 버튼 비활성화만 믿지 말고 저장 함수 진입부에서 한 번 더 권한을 검증한다.

### 8. `수정&저장`과 `신규저장`의 의미가 섞임

출장 행을 선택한 상태에서 저장 버튼을 누를 때 기존 행을 수정할지, 새 출장으로 등록할지 사용자가 혼동할 수 있었다. 이 때문에 선택된 행이 의도치 않게 덮어써지는 UX 위험이 있었다.

재발 방지:

- `수정&저장`은 선택된 행만 수정한다.
- `신규저장`은 선택된 행이 있어도 새 출장 일정으로 등록한다.
- 버튼 문구와 실제 동작이 완전히 일치하는지 수동 테스트한다.

### 9. 출장 기간 겹침 검사에서 자기 자신을 충돌 대상으로 잡음

기존 출장을 수정할 때는 현재 수정 중인 출장 행을 겹침 검사에서 제외해야 한다. 이 처리가 없으면 날짜를 바꾸지 않고 저장해도 자기 자신과 겹친다고 판단할 수 있다.

재발 방지:

- 수정 시 `excludeTripId`를 넘겨 자기 자신은 검사에서 제외한다.
- 신규 저장과 수정 저장의 겹침 검사 조건을 분리한다.
- 겹침 오류에는 충돌한 출장 기간, 국가, 업체명을 함께 표시한다.

### 10. 엑셀 업로드에서 한 행의 오류가 전체 업로드를 막을 수 있었음

출장 엑셀 업로드는 현장 데이터가 섞여 들어올 가능성이 높다. 필수값 누락, 날짜 오류, 직원 미존재, 출장 기간 겹침이 있어도 가능한 행은 계속 등록되어야 한다. 처음부터 전체 성공/실패로만 보면 운영자가 어디를 고쳐야 하는지 알기 어렵다.

재발 방지:

- 업로드는 행 단위로 검증하고 실패 행은 skip 또는 failed로 분리한다.
- 필수값 누락과 기간 겹침은 전체 중단이 아니라 해당 행 skip으로 처리한다.
- 업로드 결과는 등록/스킵/실패 건수와 대표 메시지를 토스트로 보여준다.
- 업로드 양식은 `ref/businesstrip_up.xlsx` 기준으로 유지한다.

### 11. 업체명 입력 방식을 드롭다운으로만 고정하려 함

방문업체명은 실제 운영에서 새 값이나 예외 값이 자주 생길 수 있다. 드롭다운만 강제하면 등록되지 않은 업체를 입력하기 어렵다. 반대로 완전 자유 입력만 두면 기존 업체명을 참고하기 불편하다.

재발 방지:

- 방문업체명은 텍스트 입력으로 둔다.
- `업체명 참조` 드롭다운은 기존 업체명을 입력칸에 복사하는 보조 기능으로만 둔다.
- 업체등록 테이블이 없거나 비어 있어도 출장 등록 자체가 막히지 않게 한다.

### 12. 구형 JavaScript 파일과 실제 진입 파일이 섞여 유지보수 혼란이 생김

초기 구조에는 `src/main.js`, `src/admin.js`, `src/trips.js`, `src/stats.js`, `src/permissions.js` 같은 구형 미사용 파일이 있었다. 실제 로직은 `src/app.js` 중심으로 통합되었는데, 오래된 파일이 남아 있으면 어느 파일을 고쳐야 하는지 혼동할 수 있다.

재발 방지:

- 실제 사용하지 않는 JS 파일은 삭제하고 문서에 삭제 사실을 남긴다.
- 주요 화면 로직은 `src/app.js`, 데이터 접근은 `src/store.js`, 인증은 `src/auth.js`로 본다.
- HTML에서 어떤 모듈을 import하는지 변경 전 확인한다.

### 13. 마이그레이션 적용 순서와 실제 Supabase 상태를 코드만 보고 추정함

저장소에 SQL 파일이 있어도 실제 Supabase 프로젝트에 적용되어 있지 않으면 브라우저에서는 오류가 난다. 특히 `position` 컬럼, `companies` 테이블, 국가 목록 확장, RLS 정책은 적용 여부에 따라 화면 동작이 크게 달라진다.

재발 방지:

- `001`부터 `009`까지 SQL Editor에서 순서대로 적용되었는지 확인한다.
- 컬럼/테이블 누락 오류는 코드 문제가 아니라 마이그레이션 미적용일 수 있다.
- 임시 호환 처리는 가능하지만, 최종 운영 전에는 실제 DB 스키마를 문서 기준과 맞춘다.

### 14. 필터/통계의 기준을 명확히 고정하지 않아 화면마다 결과가 달라질 수 있었음

`출장중`, `출장예정`, `출장전체`, `국내출장`, `해외출장`의 의미가 애매하면 직원 화면과 관리자 화면이 서로 다른 결과를 보여줄 수 있다. 통계도 동일 직원의 여러 출장 건을 그대로 세면 인원 수가 부풀려질 수 있다.

재발 방지:

- `출장중`은 오늘 날짜가 출장기간 안에 포함되는 출장으로 정의한다.
- `출장예정`은 시작일이 오늘보다 미래인 출장으로 정의한다.
- `출장전체`은 현재 진행 중인 출장과 미래 예정 출장을 합친다.
- 통계 인원 수는 같은 직원을 중복 계산하지 않는다.

### 15. 배포 방식과 바로가기 파일을 마지막에 따로 생각함

GitHub Pages 배포는 `master / root` 기준이고, 사용자는 `ShortCut`의 `.url` 바로가기를 통해 접근할 수 있다. 앱 기능만 완성하고 배포 주소, `.url` 템플릿, `src/config.js` 기본 설정을 나중에 확인하면 실제 사용자 PC에서 접속이 안 될 수 있다.

재발 방지:

- GitHub Pages 배포 기준 브랜치와 root 설정을 먼저 확인한다.
- `ShortCut/영일출장자현황.url.template`의 `URL=` 값을 실제 배포 주소로 바꿔 테스트한다.
- 정적 서버 로컬 테스트와 GitHub Pages 배포 테스트를 분리해서 확인한다.

## 확정된 실행 규칙

- Supabase URL과 publishable/anon key는 `src/config.js`에 둔다.
- `service_role` 키는 저장소, 브라우저 코드, `.env`에 넣지 않는다.
- 실제 Supabase에는 `001`부터 `009`까지 마이그레이션을 순서대로 적용한다.
- 직원 화면과 관리자 화면의 출장자 현황 조회 기준은 동일하게 유지한다.
- 직원은 본인 출장만 수정/삭제할 수 있고, 관리자는 전체 출장에 대해 등록/수정/삭제할 수 있다.
- 저장/삭제 권한은 UI 버튼 상태뿐 아니라 실제 처리 직전에 다시 검사한다.
- 출장 기간 겹침 검사는 같은 직원의 active 출장만 대상으로 하고, 수정 중인 자기 자신은 제외한다.
- `수정&저장`은 선택 행 수정, `신규저장`은 새 행 등록으로 고정한다.
- 엑셀 업로드는 행 단위로 검증하고 가능한 행은 계속 처리한다.
- 방문업체명은 텍스트 입력으로 두고, 업체명 참조 드롭다운은 보조 입력 기능으로 사용한다.
- 구형 미사용 JS 파일은 되살리지 않고 `src/app.js`, `src/store.js`, `src/auth.js` 중심 구조를 유지한다.
- 문서 변경이 필요한 결정은 `PROJECT_CONTEXT.md`, `README.md`, `docs/Supabase_setup.md`에 함께 반영한다.

## 다음 작업 전 체크리스트

- [ ] `src/config.js`에 실제 Supabase Project URL과 publishable/anon key가 들어 있는가?
- [ ] `service_role` 키가 저장소와 브라우저 코드에 들어 있지 않은가?
- [ ] Supabase SQL Editor에서 `001`~`009` 마이그레이션이 순서대로 적용되었는가?
- [ ] 관리자 Auth 계정 `admin@project.local`이 있고 `employees`의 관리자 행과 연결되어 있는가?
- [ ] 직원 ID 로그인 시 `employees.login_id` 조회, Auth 로그인, profile 조회가 모두 성공하는가?
- [ ] 일반 직원 화면에서 전체 출장자 현황이 보이는가?
- [ ] 일반 직원이 타인 출장을 수정/삭제할 수 없는가?
- [ ] 관리자가 전체 직원의 출장을 등록/수정/삭제할 수 있는가?
- [ ] 출장 수정 시 자기 자신 때문에 겹침 오류가 발생하지 않는가?
- [ ] 신규 저장 시 기존 선택 행이 덮어써지지 않는가?
- [ ] 엑셀 업로드에서 필수값 누락, 날짜 오류, 기간 겹침 행이 skip 처리되는가?
- [ ] 엑셀 다운로드가 현재 필터 조건 기준으로 생성되는가?
- [ ] 비밀번호 변경 후 Auth 비밀번호 변경과 `must_change_password` 해제가 함께 되는가?
- [ ] GitHub Pages 배포 주소와 `ShortCut` 바로가기 URL이 실제로 열리는가?

## 오늘의 결론

이번 프로젝트의 핵심 실수는 코드 문법보다 운영 전제와 권한 모델을 늦게 고정한 데서 나왔다. 정적 웹앱에서는 공개 설정과 비공개 키의 경계를 분명히 해야 하고, Supabase에서는 RLS가 곧 실제 기능의 일부다. 앞으로는 화면 요구사항을 권한 매트릭스와 마이그레이션 적용 체크리스트로 먼저 고정한 뒤, 프론트엔드 로직을 붙이는 순서로 진행한다.

