# 4페이지 단순화 개편 계획

이 문서는 `ref/` 폴더의 4개 이미지 기준으로 현재 앱을 단순한 4페이지 구조로 재편하기 위한 작업 계획입니다. 코드 수정 전 설계 정리용이며, 이후 구현은 이 문서를 기준으로 작은 단위로 진행합니다.

## 목표

- 첫 화면과 관리자 화면을 명확히 분리합니다.
- 직원은 로그인 후 본인 출장 등록과 출장자 현황 확인만 빠르게 수행합니다.
- 관리자는 출장 데이터 관리와 부서/직원 관리를 별도 화면에서 수행합니다.
- 기존 복잡한 탭형 대시보드를 레퍼런스 이미지처럼 단순한 좌측 입력 영역 + 우측 리스트 영역 중심으로 정리합니다.

## 기준 레퍼런스

| 파일 | 페이지명 | 목적 |
|------|----------|------|
| `ref/1.Login Page.jpg` | 로그인 페이지 | 직원 로그인 및 관리자 로그인 진입 |
| `ref/2.Business Trip.jpg` | 직원 출장 페이지 | 직원 출장 등록, 출장 현황 조회 |
| `ref/3.Admin-Business Trip.jpg` | 관리자 출장 페이지 | 관리자의 전체 출장 등록/수정/조회 |
| `ref/4.Admin-Manage.jpg` | 관리자 관리 페이지 | 부서 관리, 직원 등록, 직원 리스트 |

## 전체 페이지 구조

### 1. 로그인 페이지

레퍼런스: `ref/1.Login Page.jpg`

#### 화면 구성

- 좌측 상단:
  - `Youngil Business Trip`
  - `영일 출장자 현황 조회 사이트`
- 우측 상단:
  - `관리자 페이지 로그인`
- 중앙:
  - ID 입력
  - PASSWORD 입력
  - 비밀번호 보기/숨김 아이콘
  - Login 버튼
- 하단:
  - `Copy Right reserved by Tom Seo, 2026`

#### 동작

- 일반 직원 로그인:
  - ID 입력 예: `yh.seo`
  - 비밀번호 입력
  - 대소문자 구분 없이 ID 정규화
  - 로그인 성공 시 직원 출장 페이지로 이동
  - 로그인 후 화면에는 실제 직원명 표시
- 관리자 로그인:
  - 우측 상단 `관리자 페이지 로그인` 클릭
  - 관리자 로그인 모드 또는 관리자 로그인 팝업 표시
  - 관리자 성공 시 관리자 출장 페이지로 이동

#### 변경 포인트

- 현재 첫페이지의 부서 선택 로그인 UI 제거 검토
- 직원 로그인은 ID + 비밀번호 중심으로 단순화
- 관리자 로그인 진입은 우측 상단 텍스트 링크 형태로 유지

## 2. 직원 출장 페이지

레퍼런스: `ref/2.Business Trip.jpg`

#### 화면 구성

- 좌측 상단:
  - `Youngil Business Trip`
  - `영일 출장자 현황`
- 상단 중앙:
  - 현재 접속자 표시
  - 예: `현재접속자: 서윤환 (차장, 관리팀)`
  - `[로그아웃]`
- 좌측 입력 폼:
  - 출장등록
  - 출장시작일
  - 출장종료일
  - 국가
  - 드롭다운에 기본 등록된 국가는 가나다 순서: `미국, 중국, 캐나다, 멕시코, 브라질, 인도, 인도네시아, 포르투갈, 폴란드, 슬로바키아, 인도, 일본, 프랑스, 이탈리아, 사우디아라비아, 영국, 독일, 러시아`
  - 방문업체명
  - 도시명(선택)
  - 비고(선택)
  - 저장
  - 초기화
- 우측 현황 영역:
  - 안내 문구
  - 출장전체 / 해외출장 / 국내출장 필터 버튼
  - 출장 리스트 테이블
  - 페이지네이션

#### 테이블 컬럼

- 이름
- 직급
- 부서명
- 국가
- 업체명
- 출장기간
- 비고

#### 동작

- 직원은 본인 출장만 등록/수정 가능
- 직원은 전체 출장 현황 또는 허용된 범위의 출장 현황을 볼 수 있음
- 해외출장/국내출장 필터:
  - 국내: 국가가 `국내` 또는 사내 기준 국내 항목
  - 해외: 국내가 아닌 국가
- 출장기간은 `N일간 (YYYY-MM-DD ~ YYYY-MM-DD)` 형식
- 출장 종료 후 추가 출장이나 변경 사항이 있으면 비고에 기록하도록 안내

#### 변경 포인트

- 기존 탭형 `출장 등록/조회`, `현재 출장자`, `통계`, `비밀번호 변경` 구조를 직원 페이지에서는 감춤 또는 제거
- 직원용 통계 화면은 이번 4페이지 단순화 범위에서 제외하거나 후순위로 이동

## 3. 관리자 출장 페이지

레퍼런스: `ref/3.Admin-Business Trip.jpg`

#### 화면 구성

- 좌측 상단:
  - `Youngil Business Trip`
  - `영일 출장자 현황`
- 상단 중앙:
  - 현재 접속자 표시
  - 예: `현재접속자: admin (시스템관리자, 관리팀)`
  - `[로그아웃]`
- 우측 하단 또는 하단:
  - `관리자 페이지` 표시
- 좌측 입력 폼:
  - 부서명 선택
  - 직원명 선택
  - 직급 자동입력
  - ID 자동입력
  - E-mail 자동입력
  - 출장시작일
  - 출장종료일
  - 국가
  - 방문업체명
  - 도시명(선택)
  - 비고(선택)
  - 저장
  - 초기화
- 우측 현황 영역:
  - 출장전체 / 해외출장 / 국내출장 필터
  - 출장 리스트 테이블
  - 페이지네이션

#### 동작

- 관리자는 직원 선택 후 해당 직원의 출장 정보를 등록/수정 가능
- 직원명 선택 시 아래 값 자동 채움:
  - 직급
  - ID
  - E-mail
  - 부서 정보
- 관리자는 전체 출장 데이터를 조회 가능
- 관리자 출장 페이지에서 `관리자 관리 페이지`로 이동할 수 있는 링크/버튼 제공

#### 변경 포인트

- 현재 관리자 탭 내부의 출장 관리 흐름을 독립 페이지처럼 정리
- 관리자에게만 보이는 출장 입력 필드 추가
- 직원 선택 기반으로 출장 등록 대상이 바뀌어야 함

## 4. 관리자 관리 페이지

레퍼런스: `ref/4.Admin-Manage.jpg`

#### 화면 구성

- 좌측 상단:
  - `Youngil 부서 및 직원 관리`
  - `영일 출장자 현황`
- 상단 중앙:
  - 현재 접속자 표시
  - `[로그아웃]`
- 좌측 영역: 부서관리
  - 부서명 입력
  - 추가 버튼
  - 부서 리스트
  - 사용 여부 체크
  - 수정 버튼
  - 삭제 버튼
- 중앙 영역: 사원 정보 등록
  - 부서 선택
  - 직급 선택
  - ID 입력
  - 직원명 입력
  - 초기 비밀번호 입력
  - 직원정보 수정 & 저장 버튼
- 우측 영역: 사원 리스트
  - 순서
  - ID
  - 이름
  - 직급
  - 부서명
  - 직원 다운로드
  - 직원 업로드
  - 페이지네이션

#### 동작

- 부서 추가 시 직원 정보 등록의 부서 선택 목록에 즉시 반영
- 부서 삭제는 실제 삭제보다 비활성화 우선
- 직원 리스트에서 직원을 클릭하면 중앙 `사원 정보 등록` 폼에 값 자동 입력
- 중앙 폼은 신규 등록과 기존 직원 수정에 모두 사용
- 저장하면 우측 사원 리스트에 즉시 반영
- 직원 업로드/다운로드는 현재 Excel 호환 형식 유지

#### 변경 포인트

- 현재 직원 표의 행별 인라인 편집은 레퍼런스 기준으로 단순화
- 직원 수정은 리스트 클릭 → 중앙 폼 수정 → 저장 방식으로 통일
- Auth Email/Auth User ID는 화면에서 숨김 유지
- 직원 등록 필수값:
  - 부서
  - 직급
  - ID
  - 직원명
  - 초기 비밀번호

## 데이터 모델 변경 검토

### 직원

현재 필요한 사용자 표시/관리 필드:

- `department_id`
- `login_id`
- `name`
- `role`
- `position` 또는 `job_title`
- `login_email`
- `auth_user_id`
- `is_active`
- `must_change_password`

#### 추가 검토

- `position` 또는 `job_title` 컬럼 필요
  - 레퍼런스에는 `차장`, `대리`, `부장`, `사원` 같은 직급이 핵심 컬럼으로 표시됨
  - 기존 `role`은 권한이므로 직급으로 사용하면 안 됨
- 업체명 드롭다운 기준 데이터 필요 여부
  - 현재 `company_name`은 텍스트 입력 중심
  - 레퍼런스는 `업체명 선택` 드롭다운 형태
  - 초기 버전에서는 텍스트 입력 유지 가능, 후속으로 업체 마스터 테이블 검토

## 네비게이션 흐름

```text
로그인 페이지
  ├─ 직원 로그인 성공 → 직원 출장 페이지
  └─ 관리자 로그인 성공 → 관리자 출장 페이지

직원 출장 페이지
  └─ 로그아웃 → 로그인 페이지

관리자 출장 페이지
  ├─ 관리자 관리 페이지 이동
  └─ 로그아웃 → 로그인 페이지

관리자 관리 페이지
  ├─ 관리자 출장 페이지 이동
  └─ 로그아웃 → 로그인 페이지
```

## 구현 순서

### 1단계: 화면 구조 정리

- 로그인 페이지를 레퍼런스 기준으로 단순화
- 직원/관리자 로그인 진입 방식 확정
- 기존 탭 UI 제거 또는 내부적으로 숨김 처리

### 2단계: 직원 출장 페이지 구현

- 좌측 출장 등록 폼 재배치
- 우측 출장 리스트 테이블 구현
- 출장전체/해외출장/국내출장 필터 구현
- 페이지네이션 구현

### 3단계: 관리자 출장 페이지 구현

- 관리자 전용 직원 선택 폼 구현
- 직원 선택 시 직급/ID/E-mail 자동 표시
- 선택 직원 기준 출장 등록/수정
- 전체 출장 리스트와 필터 구현

### 4단계: 관리자 관리 페이지 구현

- 부서관리 영역 재구성
- 직원 등록/수정 폼 재구성
- 직원 리스트 클릭 → 폼 채우기 구현
- 직원 업로드/다운로드 유지 및 컬럼 정리

### 5단계: 데이터/문서 정리

- 직급 컬럼 추가 여부 결정 및 마이그레이션 작성
- Supabase 설정 문서 갱신
- TODO/WORK_LOG 갱신
- 기존 화면/기능 중 제거되는 항목 정리

## 우선순위

1. 로그인 페이지 단순화
2. 직원 출장 페이지 단순화
3. 관리자 관리 페이지 단순화
4. 관리자 출장 페이지 정리
5. 직급/업체명/페이지네이션 등 보조 데이터 개선

## 구현 시 주의사항

- 코드 변경은 페이지 단위로 작게 진행합니다.
- 기존 Supabase Auth/RLS 구조는 유지합니다.
- 직원 비밀번호는 DB에 저장하지 않습니다.
- 삭제는 원칙적으로 비활성화 처리합니다.
- 관리자에게 불필요한 내부값(Auth UID 등)은 화면에서 숨깁니다.
- 레퍼런스 이미지는 와이어프레임 기준이며, 실제 UI는 현재 앱의 웹 접근성/반응형을 유지합니다.

## 이번 개편에서 제외할 것

- 상세 통계 대시보드 고도화
- 업체 마스터 테이블 신규 설계
- 복잡한 권한 체계 추가
- 완전한 `.xlsx` 바이너리 생성
- 서버 사이드 Edge Function 도입

## 미결정 사항

- 직원 로그인에서 부서 선택을 완전히 제거할지 여부
  - 레퍼런스 로그인은 ID/PASSWORD만 있음
  - 현재 요구사항은 최근에 부서+ID+비밀번호였음
  - 4페이지 개편에서는 ID/PASSWORD만으로 단순화하는 방향을 우선 검토
- 직급 데이터 관리 방식
  - 고정 드롭다운: 사원, 대리, 과장, 차장, 부장 등
  - 또는 관리자 관리 페이지에서 직급 목록도 관리
- 방문업체명 입력 방식
  - 현재 텍스트 입력
  - 레퍼런스는 드롭다운 선택
